# คู่มือการตั้งค่า Server สำหรับบันทึกข้อมูลข้ามเครื่อง

## ปัญหาเดิม
ข้อมูลถูกบันทึกไว้ใน `localStorage` ของเบราว์เซอร์ ซึ่งทำให้ข้อมูลสามารถใช้งานได้เฉพาะในเครื่องนั้นเท่านั้น เมื่อเปลี่ยนเครื่องเปิดเว็บ ข้อมูลจะไม่แสดง

## วิธีแก้ไข
ได้เพิ่มการบันทึกข้อมูลลงในเซิร์ฟเวอร์ โดยข้อมูลจะถูกบันทึกทั้งใน localStorage (สำหรับใช้งานแบบ offline) และเซิร์ฟเวอร์ (สำหรับใช้งานข้ามเครื่อง)

## การติดตั้งและใช้งาน

### 1. ติดตั้ง Node dependencies
```bash
npm install
```

### 2. เริ่มต้น Express API Server
```bash
npm run api
```

Server จะรันที่ `http://localhost:8000`

### 3. เปิดเว็บแอปพลิเคชัน
เปิดเว็บแอปพลิเคชันในเบราว์เซอร์ตามปกติ

## การทำงาน

### เมื่อบันทึกข้อมูล:
1. ข้อมูลจะถูกบันทึกลงใน localStorage (ใช้งานได้ทันที)
2. ข้อมูลจะถูกบันทึกลงในเซิร์ฟเวอร์ (สำหรับใช้งานข้ามเครื่อง)
3. ถ้าบันทึกลงเซิร์ฟเวอร์สำเร็จ จะแสดงข้อความ "บันทึกทั้งในเครื่องและเซิร์ฟเวอร์"
4. ถ้าเซิร์ฟเวอร์ไม่ทำงาน ข้อมูลจะยังถูกบันทึกใน localStorage ไว้ใช้งาน

### เมื่อโหลดข้อมูล:
1. ระบบจะพยายามโหลดข้อมูลจากเซิร์ฟเวอร์ก่อน (ถ้ามีรหัสลูกค้า)
2. ถ้าเซิร์ฟเวอร์ไม่ตอบสนองหรือไม่มีข้อมูล จะโหลดจาก localStorage แทน
3. เมื่อเปิดเว็บครั้งแรก ระบบจะโหลดข้อมูลจาก localStorage ก่อน แล้วพยายามอัพเดทจากเซิร์ฟเวอร์

## โครงสร้างข้อมูล
ข้อมูลถูกบันทึกในโฟลเดอร์ `saved_data/` โดยใช้รหัสลูกค้าเป็นชื่อไฟล์:
- `saved_data/{customerCode}.json`

## API Endpoints

### POST `/api/save-data`
บันทึกข้อมูลลงในเซิร์ฟเวอร์
```json
{
  "customerCode": "CUST001",
  "home": {...},
  "stationAccessory": {...}
}
```

### GET `/api/load-data/<customer_code>`
โหลดข้อมูลจากเซิร์ฟเวอร์
Response:
```json
{
  "success": true,
  "data": {...}
}
```

### GET `/api/list-customers`
ดูรายการรหัสลูกค้าทั้งหมดที่มีข้อมูลบันทึกไว้

### GET `/api/health`
ตรวจสอบสถานะ API

## หมายเหตุ
- ถ้าเซิร์ฟเวอร์ไม่ทำงาน แอปจะยังคงใช้งานได้ด้วยข้อมูลจาก localStorage
- ข้อมูลจะถูกบันทึกทั้งในเครื่องและเซิร์ฟเวอร์เพื่อความปลอดภัย
- ข้อมูลในเซิร์ฟเวอร์จะถูกอัพเดททุกครั้งที่บันทึก

